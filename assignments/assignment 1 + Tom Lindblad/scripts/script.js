let foo = 0;
let bar = 0;
let sum = null;

sum = (foo / bar);

if (isNaN(sum) || sum <= -0.01)
    {
        alert ("You cant divide us!");
    }

else
    {
        console.log (sum); 
    }

function divide()
    {
        console.log ("divide");
    }

